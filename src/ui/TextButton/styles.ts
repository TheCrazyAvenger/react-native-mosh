import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  submitButton: {
    borderRadius: 50,
    marginTop: 15,
  },
  submitLabel: {
    fontFamily: 'Roboto-Bold',
    fontSize: 16,
  },
});
